# Параметры мультисенсора на настоящем SprutHub

Наблюдение 2026-09-09, 11:19–11:22 UTC. Источник — штатный интерфейс
`https://beta.spruthub.ru`, сборка `app.16735171810a795a9f2d.js`, Sprut.hub 2
с прошивкой `3.0.0b (20131)`. Выбран Aqara `RTCGQ14LM` в разрешённой
пользователем комнате. Команды управления не выполнялись.

[Проекция ответа](2026-09-09-motion-reading.json) содержит только этот
мультисенсор: четыре сервиса и 17 характеристик. Идентификаторы, серийный номер,
имена комнаты/устройства и численные показания обезличены. Типы значений,
единицы, флаги доступа и нативные перечисления сохранены. Это сокращённый
наблюдённый ответ, не полная запись обмена и не модель всех устройств Aqara.

## Контракт, который действительно отдаёт хаб

- `MotionDetected` содержит `boolValue`; `CurrentAmbientLightLevel` —
  `doubleValue` и `lux`; `BatteryLevel` — `intValue` и `%`.
- `StatusLowBattery` имеет нативное перечисление в `control.validValues`:
  `0` / `BATTERY_LEVEL_NORMAL` / «Нет», `1` / `BATTERY_LEVEL_LOW` / «Да».
- `ChargingState` имеет перечисление `0` / `NOT_CHARGING` / «Нет»,
  `1` / `CHARGING` / «Да», `2` / `NOT_CHARGEABLE` / «Не заряжаемый».
  Название характеристики «Идет зарядка» само по себе не объясняет код `2`.
- `Identify` помечен `read: false`, `write: true`. Его `boolValue: false`
  в ответе аксессуара не делает команду показанием датчика.
- Остальные характеристики доступны для чтения: сведения об устройстве,
  имена сервисов, доступность и параметры сенсоров. Конкретный серийный номер
  в проекции заменён. Время физического измерения в этом ответе не получено.

## Проверка через MCP

На `85850c944b41043b0e5db3d140efbbcef9e8d67c` запущен настоящий stdio
MCP-процесс. После каталога комнат выбрана ссылка комнаты; несколько чтений
прошли в одном процессе без переподключения. После физического воздействия
пользователя освещённость изменилась дважды. Каждое наблюдённое состояние
совпало с родным интерфейсом, ссылка характеристики сохранилась. Точные
домашние значения и результат приёмки остаются в Plan, SPRUT-2.

Для состояния, одновременно наблюдённого независимым нативным запросом и MCP,
сопоставлены все 17 значений, типов, имён и единиц. Сырые значения не потеряны,
но MCP на этом head не передаёт смысл `validValues` и включает явно нечитаемый
`Identify` в `readings`. Эти два свойства ответа нельзя считать корректной
семантикой только потому, что числа и булевы значения совпали.

Переход движения из `false` в `true` не наблюдался: в выполненных чтениях
оно оставалось `true`. Состояния разряда/зарядки сверены с нативным перечислением,
но физическая разрядка или зарядка батареи не выполнялась. Это не проверка
всех возможных состояний оборудования и не оценка задержки от события до хаба.
