# Дома и нативные сущности SprutHub, 2026-09-09

## Источники

- Публичная web-сборка SprutHub beta
  `https://beta.spruthub.ru/js/app.16735171810a795a9f2d.js`, SHA-256
  `81c1ef74ce21eb5ccff583255ba82fe766ff4cf6bc251c0566b7bd67436647f8`.
- Разрешённые read-only ответы целевого Sprut.hub 2 с прошивкой
  `3.0.0b (20131)`: account-level `hub.list`, `accessory.get` для контрольного
  аксессуара и `window.get` по возвращённому `deviceWindow`.
- Обезличенная двухдомовая подмена в продуктовом MCP-тесте. Это офлайн-модель,
  а не свидетельство наличия двух реальных домов у проверенной учётной записи.

Сырые ответы и token не сохранялись в Git. Приватный снимок окна использовался
только для структурной сверки и остаётся вне репозитория.

## Подтверждённый контракт

Account-level `hub.list` вызывается без поля `serial` и возвращает доступные
хабы, включая их native serial. Последующие RPC относятся к одному выбранному
serial. Ссылки внешнего инструмента кодируют serial как URI-компонент и не
содержат token или cid.

`accessory.get(id)` возвращает дерево service/characteristic и поля
`extensionKey`, `deviceId`, `deviceWindow`. Наблюдённый
`window.get({ windowKey: deviceWindow })` возвращает окно с options. У
контрольного датчика option `SensorDetectionSeconds` имел настроенное значение
10, тогда как native key сообщал 60. Эти значения нельзя объединять; явного
признака ожидания применения и времени источника ответ не дал.

Тип и содержимое сценария устанавливаются по полям ответа, а не по имени:
BLOCK data разбирается как JSON, GLOBAL/LOGIC может быть кодом. Диагностический
текст устройства и код сценария являются данными хаба, раскрываются только по
явному запросу и проходят редактирование похожих на секреты значений.

## Границы

На целевой учётной записи `hub.list` наблюдал один дом. Полный живой путь
`list_homes` → `inspect_home` → `get_entity`, сценарии, расширения и настройки
должен отдельно принять Architect. Архивный `device.getOptions` ответил
`Module not found`, поэтому он не является частью рабочего контракта.
